# Django settings
DEBUG=False
SECRET_KEY=generate-a-strong-secret-key-here
TEST_SERVER=False
ALLOWED_HOSTS=jar.yourdomain.com,localhost

# Database (Docker PostgreSQL)
DB_ENGINE=postgresql
DB_NAME=jar_db
DB_USER=jar_user
DB_PASSWORD=strong-database-password-here
DB_HOST=db
DB_PORT=5432

# Mailcow SMTP settings (connect to mailcow container)
EMAIL_HOST=mailcow-postfix  # or the mailcow SMTP container name
EMAIL_PORT=587
EMAIL_HOST_USER=jar@yourdomain.com
EMAIL_HOST_PASSWORD=your-mailcow-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Magyar Floorball Szakszövetség | Játékvezetői Testület <jar@yourdomain.com>

# Security
CSRF_TRUSTED_ORIGINS=https://jar.yourdomain.com
